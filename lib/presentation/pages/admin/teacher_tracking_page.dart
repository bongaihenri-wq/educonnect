// lib/presentation/pages/admin/teacher_tracking_page.dart
import 'package:flutter/material.dart';

class TeacherTrackingPage extends StatelessWidget {
  const TeacherTrackingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Suivi Enseignants')),
      body: const Center(child: Text('Suivi Enseignants - à implémenter')),
    );
  }
}