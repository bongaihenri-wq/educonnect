// lib/presentation/pages/admin/messages_page.dart
import 'package:flutter/material.dart';

class MessagesPage extends StatelessWidget {
  const MessagesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Messagerie École')),
      body: const Center(child: Text('Messagerie - à implémenter')),
    );
  }
}